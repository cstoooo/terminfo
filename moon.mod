name = "cstoooo/terminfo"

version = "0.1.0"

readme = "README.mbt.md"

repository = "https://github.com/cstoooo/terminfo"

license = "Apache-2.0"

keywords = [ "terminfo", "terminal", "curses", "cli", "ansi", "capability" ]

preferred_target = "wasm"

description = "terminfo(5) terminal capability library in pure MoonBit: compiled binary format, source language, and tparm parameter expansion"
